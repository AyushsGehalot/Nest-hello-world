<DOCTYPE html>
    <html>
        <head>
            <meta charset="utf-8">
            <title>Nice Location</title>
        </head>
        <body>
            <h1>Nice Locations</h1>
            <ul>
                {{#each locations}}
                    <li>
                        {{this.name}} - {{this.city}} - {{this.state}} - {{this.country}} - {{this.zip}}
                    </li>
                {{/each}}
            </ul>
        </body>
    </html>
</DOCTYPE>